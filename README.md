<!-- TODO： -->
<!-- import { Express } from 'mas-server';
export const useModelApi = (app: Express.Application) => {
  useModel({
    name: 'adminStudent',
    form: 'student',
    methods: ['select', 'update', 'delete', 'insert'],
    // 权限
    permission: ['admin'],
  });
}; -->
